#ifndef _CONSTANTES_H
#define _CONSTANTES_H

#define DECIMALS_NUMBER 4
#define SCALE 10000.0 // 10^4

#define size_order (400+1)
#define size_uuid (36+1)

#define size_buffer (500+1)

#define size_config_name (200+1)
#define size_root (200+1)
#define size_mac_adress (16+1)
#define size_counter_name (200+1)

#define size_process (30+1)

#define MAX_Power 100

typedef char ConfigurationName[size_config_name];
typedef char MACadress[size_mac_adress];

#endif
